import { StyleSheet } from "react-native";

import { METRICS } from "../../../themes/Metrics";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    gap: METRICS.formWrapper.gap,
  },
});

export default styles;
